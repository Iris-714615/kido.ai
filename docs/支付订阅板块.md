# 家长端订阅制付费功能 - 完整开发流程

## 一、付费模式设计

### 1.1 订阅套餐方案

| 套餐 | 月费 | 年费 | 适用场景 |
|------|------|------|----------|
| **免费版** | ¥0 | ¥0 | 试用，每日3次探索 |
| **月度标准版** | ¥29 | - | 单孩，无限探索+成长报告 |
| **年度标准版** | - | ¥299（省¥49） | 单孩，无限探索+成长报告 |
| **月度家庭版** | ¥49 | - | 最多3孩，含视频沟通 |
| **年度家庭版** | - | ¥499（省¥89） | 最多3孩，含视频沟通 |

### 1.2 功能权限对比

| 功能 | 免费版 | 标准版 | 家庭版 |
|------|--------|--------|--------|
| AI探索识别 | 3次/天 | 无限 | 无限 |
| AI对话 | 10条/天 | 无限 | 无限 |
| 成长报告 | 基础统计 | 完整版 | 完整版 |
| 实时定位 | ❌ | ❌ | ✅ |
| 视频沟通 | ❌ | ❌ | ✅ |
| 绑定儿童数 | 1 | 1 | 3 |
| 历史数据保留 | 7天 | 90天 | 永久 |
| 优先客服 | ❌ | ❌ | ✅ |

### 1.3 支付方式
- **支付宝**（电脑网站支付 / 手机网站支付）
- **微信支付**（Native支付 - 扫码 / JSAPI - 公众号）

---

## 二、技术架构设计

```
┌─────────────────────────────────────────────────────────┐
│                    家长端 (Vue3)                         │
│  订阅页 → 选套餐 → 选支付方式 → 扫码/跳转 → 支付结果     │
└────────────────────────┬────────────────────────────────┘
                         │
┌────────────────────────▼────────────────────────────────┐
│              后端 API (FastAPI)                          │
│                                                          │
│  ┌─────────────┐  ┌──────────────┐  ┌────────────────┐ │
│  │ 订阅管理     │  │ 支付服务      │  │ 权限拦截中间件  │ │
│  │ /subscription│  │ /payment     │  │ 检查订阅状态    │ │
│  └─────────────┘  └──────┬───────┘  └────────────────┘ │
│                          │                               │
│         ┌────────────────┼────────────────┐              │
│         ▼                ▼                ▼              │
│  ┌──────────────┐ ┌──────────────┐ ┌──────────────┐    │
│  │ 支付宝SDK     │ │ 微信支付SDK   │ │ 订单数据库    │    │
│  │ alipay-sdk   │ │ wechatpayv3  │ │ subscriptions │    │
│  └──────────────┘ └──────────────┘ │ orders        │    │
│                                    └──────────────┘    │
└─────────────────────────────────────────────────────────┘
```

---

## 三、详细开发流程（共7个阶段）

### 阶段1：数据库设计（数据模型）

**新增3张表：**

```python
# app/models.py 新增

class SubscriptionPlan(Base, TimestampMixin):
    """订阅套餐定义表"""
    __tablename__ = "subscription_plans"
    
    id: Mapped[int] = mapped_column(primary_key=True, autoincrement=True)
    code: Mapped[str] = mapped_column(String(32), unique=True, nullable=False)  # free/monthly_standard/yearly_standard/...
    name: Mapped[str] = mapped_column(String(64), nullable=False)
    tier: Mapped[str] = mapped_column(String(20), nullable=False)  # FREE/STANDARD/FAMILY
    billing_cycle: Mapped[str] = mapped_column(String(20), nullable=False)  # MONTHLY/YEARLY/NONE
    price_cents: Mapped[int] = mapped_column(Integer, nullable=False)  # 分为单位
    max_children: Mapped[int] = mapped_column(Integer, default=1)
    features_json: Mapped[dict] = mapped_column(JSON, default=dict)
    is_active: Mapped[bool] = mapped_column(default=True)


class Subscription(Base, TimestampMixin):
    """用户当前订阅状态表"""
    __tablename__ = "subscriptions"
    
    id: Mapped[int] = mapped_column(primary_key=True, autoincrement=True)
    user_id: Mapped[int] = mapped_column(ForeignKey("users.id"), unique=True, nullable=False)
    plan_id: Mapped[int] = mapped_column(ForeignKey("subscription_plans.id"), nullable=False)
    status: Mapped[str] = mapped_column(String(20), nullable=False)  # ACTIVE/EXPIRED/CANCELLED
    start_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), nullable=False)
    expire_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), nullable=False)
    auto_renew: Mapped[bool] = mapped_column(default=False)
    
    plan = relationship("SubscriptionPlan")
    orders = relationship("PaymentOrder", back_populates="subscription")


class PaymentOrder(Base, TimestampMixin):
    """支付订单表"""
    __tablename__ = "payment_orders"
    
    id: Mapped[int] = mapped_column(primary_key=True, autoincrement=True)
    order_no: Mapped[str] = mapped_column(String(64), unique=True, index=True, nullable=False)
    user_id: Mapped[int] = mapped_column(ForeignKey("users.id"), nullable=False)
    subscription_id: Mapped[int] = mapped_column(ForeignKey("subscriptions.id"), nullable=False)
    plan_id: Mapped[int] = mapped_column(ForeignKey("subscription_plans.id"), nullable=False)
    amount_cents: Mapped[int] = mapped_column(Integer, nullable=False)
    channel: Mapped[str] = mapped_column(String(20), nullable=False)  # ALIPAY/WECHAT
    status: Mapped[str] = mapped_column(String(20), nullable=False, default="PENDING")  # PENDING/PAID/FAILED/REFUNDED
    trade_no: Mapped[str | None] = mapped_column(String(128), nullable=True)  # 第三方流水号
    paid_at: Mapped[datetime | None] = mapped_column(DateTime(timezone=True), nullable=True)
    raw_notify_json: Mapped[dict] = mapped_column(JSON, default=dict)
    
    subscription = relationship("Subscription", back_populates="orders")
    plan = relationship("SubscriptionPlan")
```

---

### 阶段2：后端 - 套餐与订阅管理 API

**新增文件 `app/api/v1/subscription.py`：**

```
GET  /api/v1/subscription/plans              → 获取所有套餐列表
GET  /api/v1/subscription/current            → 获取当前用户订阅状态
POST /api/v1/subscription/cancel             → 取消订阅（到期不续费）
GET  /api/v1/subscription/orders             → 订单历史
```

---

### 阶段3：后端 - 支付服务集成

**新增文件 `app/services/payment.py`：**

```python
# 支付宝配置（沙箱/正式）
ALIPAY_APP_ID=2021000xxxxxxxxx
ALIPAY_PRIVATE_KEY=MIIEvQIB...
ALIPAY_PUBLIC_KEY=MIIBIjAN...
ALIPAY_GATEWAY=https://openapi-sandbox.dl.alipaydev.com/gateway.do
ALIPAY_NOTIFY_URL=https://yourdomain.com/api/v1/payment/alipay/notify
ALIPAY_RETURN_URL=https://yourdomain.com/subscription/result

# 微信支付配置
WECHAT_APP_ID=wx0xxxxxxxxx
WECHAT_MCH_ID=16xxxxxx
WECHAT_API_V3_KEY=32位密钥
WECHAT_PRIVATE_KEY=apiclient_key.pem内容
WECHAT_CERT_SERIAL_NO=证书序列号
WECHAT_NOTIFY_URL=https://yourdomain.com/api/v1/payment/wechat/notify
```

**新增文件 `app/api/v1/payment.py`：**

```
POST /api/v1/payment/create-order            → 创建支付订单（返回支付参数）
POST /api/v1/payment/alipay/notify           → 支付宝异步回调
POST /api/v1/payment/wechat/notify           → 微信异步回调
GET  /api/v1/payment/order/{order_no}        → 查询订单状态（前端轮询）
```

**支付流程时序：**

```
家长端                    后端API                 支付宝/微信
  │                        │                        │
  │ 1.选择套餐+支付方式     │                        │
  ├───────────────────────>│                        │
  │                        │ 2.创建订单(DB)          │
  │                        │ 3.调用SDK下单           │
  │                        ├───────────────────────>│
  │                        │ 4.返回支付链接/二维码    │
  │                        <───────────────────────┤
  │ 5.返回支付参数          │                        │
  │<───────────────────────┤                        │
  │                        │                        │
  │ 6.跳转支付页面/扫码     │                        │
  ├────────────────────────────────────────────────>│
  │                        │                        │
  │                        │ 7.异步通知支付结果       │
  │                        <───────────────────────┤
  │                        │ 8.验签+更新订单+激活订阅 │
  │                        │                        │
  │ 9.轮询订单状态          │                        │
  ├───────────────────────>│                        │
  │ 10.返回已支付           │                        │
  │<───────────────────────┤                        │
```

---

### 阶段4：后端 - 权限拦截中间件

**修改 `app/dependencies.py`，新增订阅校验依赖：**

```python
def get_subscription_status(user, db) -> dict:
    """返回当前订阅状态和功能权限"""
    # 查询 subscription 表
    # 返回 {tier, max_children, features, is_active, expire_at}
```

**在现有接口中加入配额检查：**
- `explore.py` - 探索接口检查每日次数
- `chat.py` - 对话接口检查每日次数
- `parent.py` - 绑定孩子数检查

---

### 阶段5：前端 - 订阅页面 UI

**家长端 `main.js` 新增订阅页组件：**

```
┌─────────────────────────────┐
│  💎 升级会员                 │
├─────────────────────────────┤
│  当前：免费版（剩余2次探索）  │
├─────────────────────────────┤
│  ┌─────────┐ ┌─────────┐   │
│  │ 标准版   │ │ 家庭版   │   │
│  │ ¥29/月  │ │ ¥49/月  │   │
│  │ 无限探索 │ │ 含视频   │   │
│  └─────────┘ └─────────┘   │
├─────────────────────────────┤
│  计费周期：[月付] [年付省30%] │
├─────────────────────────────┤
│  支付方式：                  │
│  ○ 支付宝  ○ 微信支付        │
├─────────────────────────────┤
│  [立即订阅 ¥299/年]          │
└─────────────────────────────┘
```

---

### 阶段6：前端 - 支付流程对接

```javascript
// 1. 创建订单
const order = await api('/payment/create-order', {
  method: 'POST',
  body: JSON.stringify({ plan_code: 'yearly_standard', channel: 'ALIPAY' })
});
// 返回 { order_no, pay_url, qr_code }

// 2. 支付宝 → 跳转到 pay_url
//    微信 → 显示 qr_code 二维码

// 3. 轮询订单状态
const timer = setInterval(async () => {
  const status = await api(`/payment/order/${order.order_no}`);
  if (status.status === 'PAID') {
    clearInterval(timer);
    showSuccess('订阅成功！');
    refreshSubscription();
  }
}, 2000);
```

---

### 阶段7：测试与部署

**测试清单：**
1. 套餐列表展示
2. 创建订单（支付宝沙箱）
3. 支付宝沙箱支付
4. 异步回调验签
5. 订阅激活
6. 权限拦截生效
7. 免费用户限额提示
8. 微信沙箱支付
9. 订阅到期处理
10. 退款流程

---

## 四、文件变更清单

| 操作 | 文件 | 说明 |
|------|------|------|
| **修改** | `app/models.py` | 新增3个数据模型 |
| **修改** | `app/schemas.py` | 新增订阅/支付相关Schema |
| **修改** | `app/core/settings.py` | 新增支付配置项 |
| **修改** | `app/dependencies.py` | 新增订阅权限校验 |
| **修改** | `app/main.py` | 注册新路由 |
| **修改** | `services/api/.env` | 新增支付密钥配置 |
| **新建** | `app/api/v1/subscription.py` | 订阅管理接口 |
| **新建** | `app/api/v1/payment.py` | 支付接口+回调 |
| **新建** | `app/services/payment.py` | 支付宝/微信SDK封装 |
| **修改** | `apps/parent-web/main.js` | 新增订阅页+支付流程 |
| **修改** | `apps/parent-web/styles.css` | 订阅页样式 |
| **新建** | `requirements.txt` | 新增 alipay-sdk-python, wechatpayv3 |

---

## 五、开发顺序建议

```
第1步：数据库模型 + Schema + 套餐种子数据
  ↓
第2步：订阅管理 API（套餐列表/当前状态）
  ↓
第3步：支付服务封装（支付宝SDK + 微信SDK）
  ↓
第4步：支付 API（创建订单/回调/查询）
  ↓
第5步：前端订阅页面 UI
  ↓
第6步：前端支付流程对接
  ↓
第7步：权限拦截 + 配额限制
  ↓
第8步：沙箱测试 → 正式上线
```

### 3.在项目中根目录下新建一个keys文件夹，把公私钥放进去

### 4.在tools下新建一个pay.py文件

```python

from datetime import datetime

from Crypto.PublicKey import RSA

from Crypto.Signature import PKCS1_v1_5

from Crypto.Hash import SHA256

from urllib.parse import quote_plus

from urllib.parse import urlparse, parse_qs

from base64 import decodebytes, encodebytes

import json

class AliPay(object):

"""

支付宝支付接口(PC端支付接口)

"""

def __init__(self, appid, app_notify_url, app_private_key_path,

alipay_public_key_path, return_url, debug=False):

self.appid = appid

self.app_notify_url = app_notify_url

self.app_private_key_path = app_private_key_path

self.app_private_key = None

self.return_url = return_url

with open(self.app_private_key_path) as fp:

self.app_private_key = RSA.importKey(fp.read())

self.alipay_public_key_path = alipay_public_key_path

with open(self.alipay_public_key_path) as fp:

self.alipay_public_key = RSA.importKey(fp.read())

if debug is True:

self.__gateway = " https://openapi.alipaydev.com/gateway.do "

else:

self.__gateway = " https://openapi.alipay.com/gateway.do "

def direct_pay(self, subject, out_trade_no, total_amount, return_url=None, **kwargs):

biz_content = {

"subject": subject,

"out_trade_no": out_trade_no,

"total_amount": total_amount,

"product_code": "FAST_INSTANT_TRADE_PAY",

# "qr_pay_mode":4

}

biz_content.update(kwargs)

data = self.build_body("alipay.trade.page.pay", biz_content, self.return_url)

return self.sign_data(data)

#查询接口

def query_pay(self, out_trade_no,return_url=None, **kwargs):

biz_content = {

"out_trade_no": out_trade_no,

# "product_code": "FAST_INSTANT_TRADE_PAY",

# "qr_pay_mode":4

}

biz_content.update(kwargs)

data = self.build_body("alipay.trade.query", biz_content, self.return_url)

return self.sign_data(data)

def build_body(self, method, biz_content, return_url=None):

data = {

"app_id": self.appid,

"method": method,

"charset": "utf-8",

"sign_type": "RSA2",

"timestamp": datetime.now().strftime("%Y-%m-%d %H:%M:%S"),

"version": "1.0",

"biz_content": biz_content

}

if return_url is not None:

data["notify_url"] = self.app_notify_url

data["return_url"] = self.return_url

return data

def sign_data(self, data):

data.pop("sign", None)

# 排序后的字符串

unsigned_items = self.ordered_data(data)

unsigned_string = "&".join("{0}={1}".format(k, v) for k, v in unsigned_items)

sign = self.sign(unsigned_string.encode("utf-8"))

# ordered_items = self.ordered_data(data)

quoted_string = "&".join("{0}={1}".format(k, quote_plus(v)) for k, v in unsigned_items)

# 获得最终的订单信息字符串

signed_string = quoted_string + "&sign=" + quote_plus(sign)

return signed_string

def ordered_data(self, data):

complex_keys = []

for key, value in data.items():

if isinstance(value, dict):

complex_keys.append(key)

# 将字典类型的数据dump出来

for key in complex_keys:

data[key] = json.dumps(data[key], separators=(',', ':'))

return sorted([(k, v) for k, v in data.items()])

def sign(self, unsigned_string):

# 开始计算签名

key = self.app_private_key

signer = PKCS1_v1_5.new(key)

signature = signer.sign(SHA256.new(unsigned_string))

# base64 编码，转换为unicode表示并移除回车

sign = encodebytes(signature).decode("utf8").replace("\n", "")

return sign

def _verify(self, raw_content, signature):

# 开始计算签名

key = self.alipay_public_key

signer = PKCS1_v1_5.new(key)

digest = SHA256.new()

digest.update(raw_content.encode("utf8"))

if signer.verify(digest, decodebytes(signature.encode("utf8"))):

return True

return False

def verify(self, data, signature):

if "sign_type" in data:

sign_type = data.pop("sign_type")

# 排序后的字符串

unsigned_items = self.ordered_data(data)

message = "&".join(u"{}={}".format(k, v) for k, v in unsigned_items)

return self._verify(message, signature)

```

### 5.在tools下的common文件中添加一个实例化alipay的方法

```python

#支付宝支付配制

#导入支付基类

from .pay import AliPay

#初始化阿里支付对象

def get_ali_object():

# 沙箱环境地址： https://openhome.alipay.com/platform/appDaily.htm?tab=info

app_id = "9021000132600978"  #  APPID （沙箱应用）

# 支付完成后，支付偷偷向这里地址发送一个post请求，识别公网IP,如果是 192.168.20.13局域网IP ,支付宝找不到，def page2() 接收不到这个请求

notify_url = "http://localhost:8000/user/callback"

# 支付完成后，跳转的地址。

return_url = "http://localhost:8000/user/callback"

merchant_private_key_path = "D:/p3/myproject/keys/private.txt" # 应用私钥

alipay_public_key_path = "D:/p3/myproject/keys/public.txt"  # 支付宝公钥

alipay = AliPay(

appid=app_id,

app_notify_url=notify_url,

return_url=return_url,

app_private_key_path=merchant_private_key_path,

alipay_public_key_path=alipay_public_key_path,  # 支付宝的公钥，验证支付宝回传消息使用，不是你自己的公钥

debug=True,  # 默认False,

)

return alipay

```

### 6.项目中的使用，在views.py中的pay中调用

```python

def get(self, request):

#      eta_time = datetime.now() + timedelta(seconds=10)

# send_msg.apply_async(eta=eta_time)

orderno = random.randint(111111,999999)

pay = get_ali_object()

# 生成支付的url

query_params = pay.direct_pay(

subject="test",  # 商品简单描述

out_trade_no=str(orderno),  # 用户购买的商品订单号（每次不一样） 20180301073422891

total_amount=float(1.00),  # 交易金额(单位: 元 保留俩位小数)

)

pay_url = " https://openapi-sandbox.dl.alipaydev.com/gateway.do?{0} ".format(query_params)

def get(self,request):

    #接收参数，

    data = request.args

    datames = {k:v for k,v in data.items()}

    #删除字典中key为sign的记录

    sign = datames.pop('sign')

    #鉴权 订单号 流水号

    pay = get_ali_object()

    flag = pay.verify(datames,sign)

    if flag == True:

        #支付宝流水号

        transaction_no = datames['trade_no']

        #订单号

        order_no = datames['out_trade_no']